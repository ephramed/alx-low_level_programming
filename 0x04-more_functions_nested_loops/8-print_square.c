#include "main.h"
/**
 * print_square - entry point
 * @size:- the length
 * prints square
 * Return: return the shape of square
 */
void print_square(int size)
{
	if (size <= 0)
	{
		_putchar('n');
	}
	else
	{
		for (i = 0; i < size; i++)
		{
			_putchar(23);
		}
		_putchar('\n');
	}
}
