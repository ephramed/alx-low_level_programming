#include <stdio.h>
/**
 * main - Entry point
 *
 * printf - out the string in the bracket to stdout
 * Return: return 0 (success)
 */
int main(void)
{
	printf("%s \n", "with proper grammar, but the outcome is a piece of art,");

	return (0);
}
